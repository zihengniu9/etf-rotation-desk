# AI 选股项目级因子规则

本文件是本项目运行和开发因子的长期准则。每次新增、修改或运行因子前，必须先按以下规则检查；如果数据不满足条件，宁可显示“未接入/样本不足”，不能用代理样本或主观判断补齐。

## 一、先判断行情模式，再运行策略

- 总页面先回答“当前行情适合什么模式”，再决定进入趋势、基本面、成长、行业主线、短线或 ETF 页面。
- 行业主线、短线主线和 ETF 热门题材是三个独立体系，不默认互相确认，也不强行合并成一个总分。
- 总页面负责市场状态和路径选择；每个因子负责自己的股票池、因子值、触发条件和历史赚钱效应。
- 趋势策略当前版本必须先过市场门控：趋势宽度不低于 8%，且全主板 R20 为正的股票占比不低于 50%。任一不满足时，不产生趋势新开仓，候选只能标为“仅观察”；阈值调整必须重新回测。

## 二、因子必须精确到明确股票池

- 日常行情与选股数据优先使用同花顺问财 `hithink-astock-selector`；没有成功安装或返回完整字段时，必须明确报错，不能悄悄切换成少量本地样本。
- 趋势因子默认覆盖沪深主板全市场，不得只用几只示例股、短线梯队或当前热门股代替。
- 主板代码范围：`000`、`001`、`002`、`003`、`600`、`601`、`603`、`605`；默认排除创业板 `300/301`、科创板 `688`、北交所及其他板块。
- 每次运行必须展示股票池范围、扫描数量、有效历史数量、数据日期和数据来源。
- 用户若指定其他股票池，可以改变范围，但页面、输出和报告必须明确标注，不能沿用“全主板”标题。

## 三、结果必须能解释“个股是否赚钱”

- 因子输出不能只有横截面排名，必须尽量落到每只股票：因子值、触发类型、触发日期、当前状态和历史信号数量。
- 趋势因子至少统计 5/10/20 个交易日的平均收益、中位数收益、胜率、样本数、盈亏比或 MAE；样本不足时显示“待样本”。
- 赚钱效应使用该股票自己的历史触发样本，不用全市场平均值冒充个股结论。
- 综合分只是排序工具，不能代替触发条件、退出条件和历史效果验证。

## 四、严格防止未来函数

- 所有因子只使用信号当日收盘前已知的数据；回测标签可以使用未来数据，但未来数据只能用于事后评估。
- 趋势信号若在收盘确认，默认下一交易日开盘入场；必须明确成交时点、持有期限和退出规则。
- 突破参考位不得包含当前 K 线；历史窗口、财报和公告必须使用当时已经披露的信息。
- 不自动填补缺失历史，不把短历史股票标成有效趋势候选。

## 五、数据不足时禁止伪造结论

- 缺少全股票池数据、历史窗口或有效触发样本时，页面必须进入明确的数据缺失状态。
- 不得把短线观察池、行业热门股、ETF 热榜或手工样本包装成全市场因子结果。
- 每次运行后要记录：数据是否完整、是否可计算赚钱效应、候选数、信号数和异常原因。

## 六、页面和代码架构

- `web/market_overview.html` 是总页面，只做行情模式判断和模块路由。
- 每个因子使用独立子页面，例如 `web/trend_engine.html`；基本面和成长因子后续按同一契约扩展。
- 因子计算放在 `src/low_buy_selector/`，数据生成脚本放在 `scripts/`，结果写入 `outputs/`，页面不得内置伪造的全市场因子结果。
- 本地 OHLCV CSV 仅作为离线回测或问财接口不可用时的明确降级输入；使用降级输入时必须在页面标注数据来源和范围。

详细说明见 [`docs/factor_operating_rules.md`](docs/factor_operating_rules.md)。
